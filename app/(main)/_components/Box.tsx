"use client"

import React from "react"
import MonkeyTyperInput from "./Input"

const TypeSectionBox = () => {
    let sampleText = "It was thought advisable for me to have my examinations in a room by myself, because the noise of the typewriter might disturb the other girls"
    const inputRef = React.useRef() as React.MutableRefObject<HTMLInputElement>
    return (
        <>
       
        </>
    )
}

export default TypeSectionBox